// import test from 'node:test'
// import spok from 'spok'
// import assert from 'node:assert/strict'

// console.log(spok.default)

// test('complex object', (t) => {
//   const person = { name: { first: 'Joe' } }
//   spok.default(assert, person, { name: { first: 'Anna' } })
// })
